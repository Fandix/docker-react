sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t fandix/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true fandix/docker-react npm run test
